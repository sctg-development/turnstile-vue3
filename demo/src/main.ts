// Entry point for the demo application.
// This file creates the root Vue app and mounts it to the DOM.
import { createApp } from 'vue';
import App from './App.vue';

// Mount the root App component into the HTML element with id="app".
createApp(App).mount('#app');